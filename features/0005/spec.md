Objetivo: criar filtros no componente que lista os pull requests na tela de register

1. Permitir filtrar pullrequest 
2. o header do componente de pullrequests deve ser do mesmo tamanho (height) do componente da linha do tempo
3. os filtros devem ter os valores pre carregados
4. os filtros devem copiar exatamente o mesmo componente de filtros do /Users/alexcarlos/repos/_iniciativas/ComandaCerta/ComandaCerta.App
5. Deve permitir filtrar por status, branch e repositorio
6. os valores pre selecionados devem trazer os valores possiveis para filtro, de acordo com os registros que existem para aquele card